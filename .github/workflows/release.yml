name: Auto Release with SQLite (Root File)

on:
    push:
        branches:
            - main

permissions:
    contents: write

jobs:
    build-and-release:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Setup Bun
              uses: oven-sh/setup-bun@v1
              with:
                  bun-version: latest

            - name: Install dependencies
              run: bun install

            - name: Remove old SQLite database
              run: |
                  echo "🧹 Removing old SQLite database..."
                  rm -f ./Holy_bible.sqlite
                  echo "✅ Old database removed."

            - name: Generate migration files
              run: |
                  echo "🧩 Generating Drizzle migrations..."
                  bunx drizzle-kit generate
              env:
                  DATABASE_URL: file:./Holy_bible.sqlite

            - name: Run DB Schema Migration (Force Recreate)
              run: |
                  echo "🏗️ Running Drizzle migration..."
                  bunx drizzle-kit push --force
                  echo "✅ Migration complete."
                  echo "🗂️ Tables created:"
                  sqlite3 Holy_bible.sqlite "SELECT name FROM sqlite_master WHERE type='table';"
              env:
                  DATABASE_URL: file:./Holy_bible.sqlite

            - name: Import Bible data into SQLite
              run: |
                  echo "📖 Importing Bible data..."
                  bun run migrate_translation.ts
              env:
                  DATABASE_URL: file:./Holy_bible.sqlite

            - name: Verify SQLite file
              run: |
                  echo "🔍 Verifying database..."
                  ls -lh ./*.sqlite
                  echo "🗂️ Tables in database:"
                  sqlite3 Holy_bible.sqlite "SELECT name FROM sqlite_master WHERE type='table';"
                  echo "📊 Row count in metadata:"
                  sqlite3 Holy_bible.sqlite "SELECT count(*) FROM metadata;"
              continue-on-error: true

            - name: Create or Update GitHub Release
              uses: softprops/action-gh-release@v2
              with:
                  tag_name: latest
                  name: "Automated Release"
                  body: "Auto release for commit ${{ github.sha }}"
                  files: |
                      ./*.sqlite
                  draft: false
                  prerelease: false
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
